name: 拉取提交
on:
  workflow_dispatch:
    inputs:
      commit-hash:
        description: "目标合并提交的哈希值"
        required: true
      source-repo:
        description: "源仓库的URL（格式：owner/repo）"
        required: true
      target-branch:
        description: "您要将更改合并到的分支名称（本地分支）"
        required: true
      git-username:
        description: "GitHub用户名（需repo权限）"
        required: true
      git-password:
        description: "GitHub密码/PAT"
        required: true
        sensitive: true

jobs:
  cherry-pick:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 我的仓库
        uses: actions/checkout@v4
        with:
          fetch-depth: 0  # 获取完整历史以便于 cherry-pick

      - name: 配置安全环境
        run: |
          echo "https://${{ inputs.git-username }}:${{ inputs.git-password }}@github.com" > .git-credentials
          git config --global credential.helper 'store --file=.git-credentials'

      - name: 设置 Git 用户信息
        run: |
          git config --global user.name "${{ inputs.git-username }}"  # 使用输入的 GitHub 用户名
          git config --global user.email "your-email@example.com"  # 替换为用户的电子邮件

      - name: 切换到目标分支
        run: |
          git checkout ${{ inputs.target-branch }}

      - name: 处理合并提交
        run: |
          echo "尝试 cherry-pick 提交: ${{ inputs.commit-hash }}"
          # 直接从源仓库 cherry-pick 特定提交
          if git fetch "https://github.com/${{ inputs.source-repo }}.git" ${{ inputs.commit-hash }} && git cherry-pick FETCH_HEAD; then
            echo "成功 cherry-pick 提交: ${{ inputs.commit-hash }}"
            # 提交更改
            git add .
            git commit -m "Cherry-picked changes from ${{ inputs.commit-hash }}"
          else
            echo "检测到冲突，处理中..."
            # 在目标分支上处理冲突
            # 获取冲突文件
            conflict_files=$(git diff --name-only --diff-filter=U)

            # 提交没有冲突的文件
            git add .
            git commit -m "Cherry-picked non-conflicting changes from ${{ inputs.commit-hash }}"

            # 提示用户手动解决冲突
            echo "请手动解决以下冲突文件，并提交更改："
            echo "${conflict_files}"

            # 中止 cherry-pick
            git cherry-pick --abort || true
          fi

      - name: 推送更改到远程分支
        run: |
          git push origin ${{ inputs.target-branch }}
